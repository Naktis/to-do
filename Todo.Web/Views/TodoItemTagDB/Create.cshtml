@model Todo.Web.Models.TodoItemTag

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>TodoItemTag</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="TodoItemID" class="control-label"></label>
                <select asp-for="TodoItemID" class="form-control" asp-items="ViewBag.TodoItemID">
                    <option selected value="">Choose TodoItem</option>
                </select>
            </div>
            <div class="form-group">
                <label asp-for="TagID" class="control-label"></label>
                <select asp-for="TagID" class="form-control" asp-items="ViewBag.TagID">
                    <option selected value="">Choose Tag</option>
                </select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
